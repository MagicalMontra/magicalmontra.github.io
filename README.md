# MagicalMontra.github.io

Portfolio site for Setthakritti Duangjamsai.

## Structure

```text
.
├── index.html
├── css/
│   └── style.css
├── projects/
│   ├── backend.html
│   ├── game-server.html
│   └── playbux.html
├── resume/
│   └── Setthakritti_Duangjamsai_Resume.pdf
└── .nojekyll
```

The site is plain HTML/CSS with no build step.

## Local preview

From the repository root:

```bash
python3 -m http.server 8000
```

Then open:

```text
http://localhost:8000
```

## Before publishing

- Replace or add direct project repository links once those repositories are public and cleaned up.
- Review all professional project wording for anything that should remain confidential.
- Update the resume file when the resume changes.

## Suggested first commit

```bash
git add .
git commit -m "Create engineering portfolio site"
```
